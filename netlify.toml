[build]
  base    = "frontend"
  command = "ng build"
  publish = "dist/financial-frontend/browser"

[functions]
  directory = "netlify/functions"  # pasta inexistente — desativa functions

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
